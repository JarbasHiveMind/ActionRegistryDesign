type: recognizer_loop:utterance
data:
  utterances: []
  # List of possible transcribed user input
  lang: ""
  # Lowercased BCP 47 Language tag associated with `utterances` (i.e. en-us)
context:
  client_name: ""
  # Human-readable name to identify the module capturing this input
  client: ""
  # Classification of the input source (api, mobile, browser, microphone)
  source: ""
  # Source module name
  destination: "skills"
  # Destination module name
  ident: ""
  # Unique ID of this user input
  timing: {}
  # Optional map of events to timestamps
  username: ""
  # Originating user's ID
  profiles: [ ]
  # Optional list of user profile data for any users associated with the input